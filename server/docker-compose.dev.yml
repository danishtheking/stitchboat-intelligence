services:
  # PostgreSQL Database
  postgres:
    image: postgres:15
    container_name: stitchboat_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: stitchboat
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_INITDB_ARGS: '--encoding=UTF-8 --lc-collate=C --lc-ctype=C'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - stitchboat_network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d stitchboat']
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    container_name: stitchboat-redis-dev
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - stitchboat_redis_data:/data
    networks:
      - stitchboat_network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
  stitchboat_redis_data:

networks:
  stitchboat_network:
    driver: bridge
